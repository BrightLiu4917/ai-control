# 安全说明

## 边界声明（诚实优先）

本工具的门禁是**工程约束，不是安全边界**：拥有文件写权限的 AI 理论上可伪造
JUnit 报告、confirmed.json 或直接改动门禁脚本。门禁的设计目标是把"张嘴谎报"
变成"必须留下可查的假证据"（成本与暴露风险数量级上升），并非密码学保证。
不要把本工具作为对抗恶意行为者的安全控制。

## 钩子的安全性质

PreToolUse 守卫脚本以 fail-open 设计（解析异常一律放行），保证不会瘫痪用户
会话；`AI_CONTROL_HOOKS=off` 可临时停用。守卫脚本零依赖、不联网、不上报。

## 漏洞报告

发现绕过路径或安全问题，请提 GitHub Issue（可标注 security），或在 issue 中
留下联系方式。历史审计与已修复的绕过路径见 docs/external-review-2026-09-11.md
与 CHANGELOG.md。
